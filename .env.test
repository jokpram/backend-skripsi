# Test Environment Variables
NODE_ENV=test
JWT_SECRET=test-secret-key-for-testing-only
PORT=3001

# Test Database (optional - use in-memory or separate test DB)
DB_HOST=localhost
DB_USER=test
DB_PASS=test
DB_NAME=cronos_test
DB_PORT=5432

# Midtrans Test Keys (sandbox)
MIDTRANS_SERVER_KEY=test-server-key
MIDTRANS_CLIENT_KEY=test-client-key
